name: CI

on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run tests
        run: ./gradlew --no-daemon test

  docker-build:
    name: Docker Build Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: false

      - name: Validate docker-compose file
        env:
          JWT_SECRET: ${{ secrets.JWT_SECRET || 'ci-dummy-jwt-secret-at-least-32-bytes' }}
          JWT_ISSUER: ${{ vars.JWT_ISSUER || vars.ISSUER_URI || 'aandiclub-auth' }}
          JWT_AUDIENCE: ${{ vars.JWT_AUDIENCE || vars.AUDIENCE || 'aandiclub-api' }}
        run: docker compose -f docker-compose.yml config
